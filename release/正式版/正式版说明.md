# 正式版交付目录

这里用于放最终可直接使用的正式版程序。

目标结构：

```text
正式版/
  桌面端/
    样品管理.exe
  手机端/
    样品采集.apk
```

当前还处于开发阶段，暂时不放假的 exe 或 apk 文件。

等打包完成后：

- 桌面端 `样品管理.exe` 放到 `桌面端/`
- 手机端 `样品采集.apk` 放到 `手机端/`

正式版必须使用 production 配置，不使用 dev 包名和 dev 数据目录。
