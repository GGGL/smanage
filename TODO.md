# TODO

## 最高优先级：正式打包 APK

当前电脑缺少 Android 打包环境，APK 未生成。下一台电脑继续时优先完成：

1. 安装或确认 JDK 17/21。
2. 安装或确认 Android Studio / Android SDK。
3. 确认 `ANDROID_HOME` 或 `ANDROID_SDK_ROOT`。
4. 确认可用 `sdkmanager`、`adb`、Gradle。
5. 为 `dev/mobile` 创建正式 Android 打包工程。
6. 使用 production 配置：
   - 应用名：`样品采集`
   - 包名：`com.gggl.smanage`
   - 数据库：`sample-mobile-db`
   - 导出文件名不要带 `_dev_`
7. 生成真实可安装 APK。
8. 输出到：

```text
release/正式版/手机端.apk
```

## 手机端后续优化

- 当前手机端仍是静态 Web 实现，后续打 APK 推荐使用 Capacitor 或原生 WebView 容器。
- 手机端导入电脑同步包已改为二段式：
  - 先选择同步包
  - 显示文件名和大小
  - 用户点击确认导入
  - 成功/失败显示明确提示
- 手机端产品详情已去掉可见“状态/待确认”，底层数据仍保留 `status` 字段。

## 电脑端后续优化

- 电脑端项目详情页已改成表格列表。
- 待确认样品支持单条确认和批量确认。
- 正式版 exe 已生成并验证。
- 后续可以增加“打开数据包目录”按钮，方便用户快速定位：
  - `数据包/手机导出给电脑/`
  - `数据包/电脑导出给手机/`

## 发布前检查

- 确认 `release/正式版/打开桌面端.exe` 可运行。
- 确认 `release/正式版/手机端.apk` 是真实 APK，不要放占位假文件。
- 用手机端采集 1 条样品，导出给电脑。
- 电脑端导入、修改样品、导出给手机。
- 手机端导入电脑同步包，确认数据更新。
